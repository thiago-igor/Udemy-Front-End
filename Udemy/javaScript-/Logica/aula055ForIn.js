/*const frutas = ['pera', 'maçã', 'Uva'];
for(let i in frutas){
    console.log(frutas[i]);
}
*/

const pessoa = {
    nome: 'luiz',
    sobrenome:'Otavio',
    idade: 30
};

for(let key in pessoa){
    console.log(key,pessoa[key])
}